# BTCNFT Protocol CLI - Environment Configuration
# Copy this file to .env (local), .env.sepolia, .env.holesky, or .env.base

# RPC URL (optional - defaults to network setting if not specified)
# Use 127.0.0.1 (not localhost) to avoid IPv6 resolution issues on macOS
# RPC_URL=http://127.0.0.1:8545

# Private key for signing transactions
# WARNING: Never commit real private keys to version control
PRIVATE_KEY=0x...

# Shared Contract
TREASURE=0x...

# WBTC Collateral Stack
WBTC=0x...
BTC_TOKEN_WBTC=0x...
VAULT_WBTC=0x...

# cbBTC Collateral Stack
CBBTC=0x...
BTC_TOKEN_CBBTC=0x...
VAULT_CBBTC=0x...

# Legacy aliases (for existing CLI commands)
BTC_TOKEN=0x...
VAULT=0x...

# Contract Addresses (Issuer Layer - Optional)
# Required for auction commands
AUCTION_CONTROLLER=0x...
